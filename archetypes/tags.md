---

title: "{{ replace .Name "-" " " | title }}"
slug: "{{ replace .Name "-" " " | title }}"
description:
#image: cover.png
date: "{{ .Date }}"
lastmod: "{{ .Date }}"
style:
 background: "#2a9d8f"
 color: "#fff"

---
